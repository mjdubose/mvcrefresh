@model Videly.Models.Customer
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>
<ul>
    <li>Rental Plan: @Model.MembershipType.MembershipTypeText</li>
    @if (Model.Birthdate == null)
    {
        
    }
     else
    {
        <li>Birth Date: @Html.Encode($"{Model.Birthdate:MM/dd/yyyy}") </li>
    }

</ul>

