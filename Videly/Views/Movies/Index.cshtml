@model List<Videly.Models.Movie>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Movies</h2>
@if (Model.Count == 0)
{
    <ul>There are no movies</ul>
}
else
{

foreach (var movie in Model)
 {
     <ul>@movie.Name</ul>
 }
}